<form method="get" action="{{ path('post_search') }}" class="d-flex mb-4">
    <input type="text" name="q" value="{{ app.request.get('q') }}" placeholder="Search posts..." class="form-control me-2" />
    <button type="submit" class="btn btn-primary">Search</button>
</form>